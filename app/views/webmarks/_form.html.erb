<%= form_with(model: webmark, local: true) do |form| %>
  <% if webmark.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(webmark.errors.count, "error") %> prohibited this webmark from being saved:</h2>

      <ul>
      <% webmark.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url, id: :webmark_url %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, id: :webmark_content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
